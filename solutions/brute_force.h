#ifndef MHE_BRUTE_FORCE_H
#define MHE_BRUTE_FORCE_H

#include "../structures/indicators_t.h"

namespace mhe {

    indicators_t brute_force(const indicators_t &problem, const graph_t &graph);

} // mhe

#endif //MHE_BRUTE_FORCE_H
